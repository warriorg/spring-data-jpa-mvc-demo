/** *  */package com.myb.infrastructure.repository;import javax.persistence.EntityManager;import javax.persistence.PersistenceContext;import javax.persistence.Query;import org.apache.commons.lang3.StringUtils;import org.slf4j.Logger;import org.slf4j.LoggerFactory;/** * @author warrior * */public class BasicRepository {		// 所有子类中都使用此logger进行日志打印, 系统对日志进行统一配置	protected Logger logger = LoggerFactory.getLogger(getClass());	// JPA实体管理器	protected EntityManager entityManager;		/*	 * (non-Javadoc)	 * 	 * @see	 * com.etmobile.health.common.dao.IBaseDao#setEntityManager(javax.persistence	 * .EntityManager)	 */	@PersistenceContext	public void setEntityManager(final EntityManager entityManager) {		this.entityManager = entityManager;	}	protected Query createQuery(final String jpql, final Object... values) {		assert StringUtils.isNotBlank(jpql);		// Query query = entityManager.createQuery(jpql, entityClass);		Query query = entityManager.createQuery(jpql);		if (values != null) {			for (int i = 0; i < values.length; i++) {				// jpa参数从1开始				query.setParameter(i + 1, values[i]);			}		}		return query;	}	}